//imports that are needed
import { Box } from "@mui/material";
import { styled } from "@mui/system";

//Codes for the box header
const FlexBetween = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});

export default FlexBetween